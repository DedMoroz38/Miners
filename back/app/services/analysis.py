from pathlib import Path


def analyze_sample(image_path: Path) -> str:
    """Анализ образца по изображению.

    Заглушка: сюда встанет вызов ML-модели (сегментация фаз, метрики,
    классификация руды). Пока возвращает фиксированный результат.
    """
    return "четко"
